apiVersion: v1
kind: Service
metadata:
  name: ndi-service
  labels:
    app: ndi-discovery-server
  namespace: ndi
  annotations:
    external-dns.alpha.kubernetes.io/hostname: ndi.svc.technotut.net
spec:
  selector:
    app: ndi-discovery-server
  type: LoadBalancer
  loadBalancerIP: 10.11.230.254
  ports:
    - name: ndi
      protocol: TCP
      port: 5959
      targetPort: 5959